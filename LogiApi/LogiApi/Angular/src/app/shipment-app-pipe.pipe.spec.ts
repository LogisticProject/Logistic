import { ShipmentAppPipePipe } from './shipment-app-pipe.pipe';

describe('ShipmentAppPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ShipmentAppPipePipe();
    expect(pipe).toBeTruthy();
  });
});
